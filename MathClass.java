package first;

public class MathClass {

	public static void main(String[] args) {
		
		double x= 3.80;
		double y = -10;
		double z;
		z = Math.max(x,y);
		
		z = Math.abs(y);
		
		z= Math.sqrt(x);
		
		z=Math.round(x);
		System.out.println(z);
	}
}
